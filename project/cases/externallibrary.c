#include <stdio.h>

//double result_x = 1.0;
void functionThatUsesPrint(int a){
    int x = 0;
    int b = 19;
    if (a >= 5) {
        printf("%d", a);
    }
    else {
        x = 10;
    }
    
}

// The pass should output 1.0 has correct value (using the matchValue function) and 1.0 using the true function on argument x

